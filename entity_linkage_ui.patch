diff --git a/app.py b/app.py
index 41b1131..9ec3adf 100644
--- a/app.py
+++ b/app.py
@@ -286,13 +286,26 @@ def upload_files():
 
         app.logger.info(f"Successfully loaded {len(preview_data)} tables with advanced pipeline")
 
+        # Detect (without mutating anything) whether a shared key like
+        # 'Patient ID' links two or more of the uploaded tables, so the UI
+        # can tell the user up front that cross-table consistency will be
+        # preserved during generation, instead of that only being visible
+        # in server logs.
+        entity_linkage = pipeline.describe_entity_linkage()
+        if entity_linkage.get('detected'):
+            app.logger.info(
+                f"Detected entity linkage via '{entity_linkage['entity_key']}': "
+                f"master={entity_linkage['master_table']}, satellites={entity_linkage['satellite_tables']}"
+            )
+
         return jsonify({
             'success': True,
             'files_uploaded': len(uploaded_files),
             'tables': preview_data,
             'schema': pipeline.schema,
             'session_id': pipeline_state['session_id'],
-            'advanced_features_enabled': True
+            'advanced_features_enabled': True,
+            'entity_linkage': entity_linkage
         })
 
     except Exception as e:
@@ -689,7 +702,8 @@ def generate_data():
                 'method_used': method_used,
                 'synthesis_required': synthesis_required,
                 'total_synthesize_columns': total_synthesize_columns,
-                'total_copy_columns': total_copy_columns
+                'total_copy_columns': total_copy_columns,
+                'entity_linkage': pipeline.describe_entity_linkage()
             })
         else:
             pipeline_state['status'] = 'error'
diff --git a/index.html b/index.html
index 9990ab0..d7ab5ca 100644
--- a/index.html
+++ b/index.html
@@ -727,6 +727,7 @@
         // Global state
         let currentStep = 1;
         let uploadedData = null;
+        let entityLinkageInfo = null;
         let generatedData = null;
         let evaluationResults = null;
         const API_BASE_URL = window.location.origin + '/api';
@@ -902,6 +903,7 @@
 
                 if (response.success) {
                     uploadedData = response.tables;
+                    entityLinkageInfo = response.entity_linkage || null;
 
                     updateStatus('Files uploaded successfully');
                     showDataPreview();
@@ -916,6 +918,41 @@
             }
         }
 
+        function renderEntityLinkageBanner() {
+            if (!entityLinkageInfo) return '';
+
+            if (entityLinkageInfo.detected) {
+                return `
+                    <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-4">
+                        <p class="font-semibold text-blue-800">🔗 Linked tables detected</p>
+                        <p class="text-sm text-blue-700 mt-1">
+                            Shared identifier <code class="bg-blue-100 px-1 rounded">${entityLinkageInfo.entity_key}</code>
+                            found in <strong>${entityLinkageInfo.master_table}</strong> (${entityLinkageInfo.entity_count} entities)
+                            and ${entityLinkageInfo.satellite_tables.length} linked table${entityLinkageInfo.satellite_tables.length === 1 ? '' : 's'}:
+                            ${entityLinkageInfo.satellite_tables.join(', ')}.
+                        </p>
+                        <p class="text-sm text-blue-700 mt-1">
+                            Each synthetic entity's rows will stay consistent across all of these tables during generation.
+                        </p>
+                    </div>
+                `;
+            }
+
+            if (entityLinkageInfo.candidate_tables) {
+                return `
+                    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-4">
+                        <p class="font-semibold text-yellow-800">⚠️ Possible shared identifier not linkable</p>
+                        <p class="text-sm text-yellow-700 mt-1">
+                            Found <code class="bg-yellow-100 px-1 rounded">${entityLinkageInfo.entity_key}</code> in
+                            ${entityLinkageInfo.candidate_tables.join(', ')}, but ${entityLinkageInfo.reason}
+                        </p>
+                    </div>
+                `;
+            }
+
+            return '';
+        }
+
         function showDataPreview() {
             const preview = document.getElementById('data-preview');
             const content = document.getElementById('preview-content');
@@ -923,6 +960,7 @@
             preview.classList.remove('hidden');
 
             content.innerHTML = `
+                ${renderEntityLinkageBanner()}
                 <div class="grid gap-4">
                     ${Object.entries(uploadedData).map(([tableName, data]) => `
                         <div class="bg-white rounded-lg p-4 border">
@@ -1086,7 +1124,7 @@
                     updateStatusIndicator('status-processing', 'Complete', 'text-green-600');
                     updateStatusIndicator('status-generation', 'Complete', 'text-green-600');
 
-                    showGenerationResults(generateResponse.summary);
+                    showGenerationResults(generateResponse.summary, generateResponse.entity_linkage);
                     document.getElementById('next-btn-3').classList.remove('hidden');
                 } else {
                     throw new Error(generateResponse.error || 'Generation failed');
@@ -1149,14 +1187,22 @@
             document.getElementById('progress-text').textContent = text;
         }
 
-        function showGenerationResults(summary) {
+        function showGenerationResults(summary, entityLinkage) {
             const resultsDiv = document.getElementById('generation-results');
             const content = document.getElementById('results-content');
 
             const totalRows = Object.values(summary).reduce((sum, table) => sum + table.rows, 0);
             const totalTables = Object.keys(summary).length;
 
+            const linkageNote = (entityLinkage && entityLinkage.detected) ? `
+                <div class="bg-blue-50 border border-blue-200 rounded-lg p-3 mb-4 text-sm text-blue-700">
+                    🔗 Cross-table consistency preserved via <code class="bg-blue-100 px-1 rounded">${entityLinkage.entity_key}</code>
+                    across ${entityLinkage.master_table} and ${entityLinkage.satellite_tables.join(', ')}.
+                </div>
+            ` : '';
+
             content.innerHTML = `
+                ${linkageNote}
                 <div class="grid md:grid-cols-3 gap-4">
                     <div class="text-center">
                         <h4 class="font-semibold">Tables Generated</h4>
diff --git a/pipeline.py b/pipeline.py
index 0d6cea2..b11abd7 100644
--- a/pipeline.py
+++ b/pipeline.py
@@ -1732,6 +1732,41 @@ class SyntheticDataPipeline:
 
         return master_table_name, selected_entities, synthetic_ids
 
+    def describe_entity_linkage(self):
+        """Non-destructive preview of cross-table entity linkage: which
+        shared key (if any) links two or more uploaded tables, which table
+        looks like the 'master' (one row per entity), and which are
+        satellites. This detection already drives generation internally
+        (see _build_entity_assignment / _generate_linked_satellite_table);
+        this method just exposes the same result so the UI can show it to
+        the user right after upload instead of it happening silently."""
+        entity_key = self._detect_shared_entity_key()
+        if not entity_key:
+            return {'detected': False}
+
+        linked_tables = [t for t, tdf in self.original_data.items() if entity_key in tdf.columns]
+        if len(linked_tables) < 2:
+            return {'detected': False}
+
+        assignment = self._build_entity_assignment(entity_key, linked_tables)
+        if not assignment:
+            return {
+                'detected': False,
+                'entity_key': entity_key,
+                'candidate_tables': linked_tables,
+                'reason': "No table has exactly one row per entity, so these tables can't be linked."
+            }
+
+        master_table_name, selected_entities, _ = assignment
+        satellite_tables = [t for t in linked_tables if t != master_table_name]
+        return {
+            'detected': True,
+            'entity_key': entity_key,
+            'master_table': master_table_name,
+            'satellite_tables': satellite_tables,
+            'entity_count': int(self.original_data[master_table_name][entity_key].dropna().nunique())
+        }
+
     def _detect_episode_date_columns(self, df):
         """Find Start/Stop/Ongoing or Begin/End style date-column groups in
         a table (the DMD episode-record and reference-period-record shapes),
